require 'spec_helper'

describe UserDecorator do
  context "#follow_link" do
    let!(:justin_bieber) { create(:user, :name => "justin bieber")}
    let!(:usa) { create(:user, :name => "usa") }
    it "see follow link" do

    end
  end
end
